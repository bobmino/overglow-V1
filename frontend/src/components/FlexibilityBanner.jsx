import React from 'react';

const FlexibilityBanner = () => {
  return (
    <section className="py-12 bg-gradient-to-r from-green-50 to-blue-50">
      <div className="container mx-auto px-4 text-center">
        <h2 className="text-3xl font-bold text-gray-900 mb-4">Keep things flexible</h2>
        <p className="text-gray-700 max-w-2xl mx-auto leading-relaxed">
          Use Reserve Now & Pay Later to secure the activities you don't want to miss without being locked in.
        </p>
      </div>
    </section>
  );
};

export default FlexibilityBanner;
